# development | production
MODE=production
NEXT_PUBLIC_API_URL=/api
API_URL=localhost:3000
PORT=3000

# Public origin used for canonical, Open Graph, JSON-LD, sitemap, robots.
# HTTPS in production, no trailing slash.
SITE_URL=
NEXT_PUBLIC_SITE_URL=